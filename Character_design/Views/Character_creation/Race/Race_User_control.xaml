<UserControl x:Class="Character_design.Views.Character_creation.Race.Race_User_control"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1146">
    <Grid Background="White">
        <Canvas HorizontalAlignment="Left" Height="800" VerticalAlignment="Center" Width="250" Background="Black">
            <TextBlock Height="36" Canvas.Left="10" TextWrapping="Wrap" Text="Выберите расу:" Canvas.Top="10" Width="230"
                       Foreground="Wheat" FontSize="20" TextAlignment="Center"/>
            <ListBox Grid.Column="0" Height="744" Canvas.Left="10" Canvas.Top="46" Width="230" 
                     ItemsSource="{Binding Races}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <Image Height="50" Width="52" Source="{Binding Path=Small_img_path}"/>
                            <TextBlock Height="26" TextWrapping="Wrap" Width="147" Foreground="Wheat" TextAlignment="Center" FontSize="16" 
                                       Text="{Binding Path=Race_name}" />

                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>
        </Canvas>
        <Image HorizontalAlignment="Left" Height="502" Margin="262,6,0,0" VerticalAlignment="Top" Width="371"/>
        <TextBlock HorizontalAlignment="Left" Height="34" Margin="645,10,0,0" TextWrapping="Wrap" Text="Описание расы:" VerticalAlignment="Top" Width="365"
                   FontSize="18"/>
        <ScrollViewer Margin="645,49,10,447" CanContentScroll="True" VerticalScrollBarVisibility="Auto">
            <TextBlock Height="294"  TextWrapping="Wrap" Width="471"
                   Text="{Binding Selected_race_description}"/>
        </ScrollViewer>




    </Grid>
</UserControl>
